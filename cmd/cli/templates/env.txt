# Give your application a unique name (no spaces)
APP_NAME=${APP_NAME}

# application url
APP_URL=http://localhost:4000

# false for production, true for development
DEBUG=true

# the port should we listen on
PORT=4000

# the server name, e.g, www.mysite.com
SERVER_NAME=localhost

# should we use https?
SECURE=false

# database config - postgres or mysql
DATABASE_TYPE=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=root
DATABASE_PASS=password
DATABASE_NAME=kabarda
DATABASE_SSL_MODE=disable

# redis config
REDIS_HOST=
REDIS_PASSWORD=
REDIS_PREFIX=${APP_NAME}

# cache (currently only redis)
CACHE=

# cooking settings
COOKIE_NAME=${APP_NAME}
COOKIE_LIFETIME=1440
COOKIE_PERSIST=true
COOKIE_SECURE=false
COOKIE_DOMAIN=localhost

# session store: cookie, redis, mysql, or postgres
SESSION_TYPE=cookie

# mail settings
MAIL_DOMAIN=
SMTP_HOST=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_PORT=1025
SMTP_ENCRYPTION=
SMTP_FROM=
SMTP_FROM_ADDRESS=

# mail settings for api services: we support mailgun, sparkpost and sendergrid
MAILER_API=
MAILER_KEY=
MAILER_URL=

# template engine: go or jet
RENDERER=jet

# the encryption key; must be exactly 32 characters long
KEY=${KEY}